package com.services;
//
//import com.dao.UserDAO;
//import com.models.User;
//
public class ServiceIdRequest {
////    public User processId(String id_input, UserDAO userDAO) {
////        int id_verified = validateId(id_input);
////        // if we arrive here, the Database should be called
////        User user = userDAO.getUserById(id_verified);
////        if (user == null) {
////            // can throw an error or
////            return null;
////        }
////        return user;
//////    }
////    private int validateId(String id_input) {
////        if (id_input == null) {
////            //throw custom error
////            throw new RuntimeException(); //replace with custom error
////        }
////        int id_
////        try {
////
////        }
////
//    }
}
